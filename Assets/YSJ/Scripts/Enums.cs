public enum PlayerStateType
{
    Idle,
    Move,
    Death,
}

public enum MonsterStateType
{
    None,
    Idle,
    Move,
    Win,
    Death,
}

public enum MonsterPattenState
{
    None,
    Play,
    Done,
}
