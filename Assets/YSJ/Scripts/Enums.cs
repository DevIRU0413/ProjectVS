namespace PVS
{
    public enum PlayerStateType
    {
        Idle,
        Move,
        Win,
        Death,
    }

    public enum MonsterStateType
    {
        None,
        Idle,
        Move,
        Win,
        Death,
    }

    public enum MonsterPatternState
    {
        None,
        Play,
        Done,
    }
}
